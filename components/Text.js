import { createComponent } from 'react-fela'

const info = () => ({
  padding: 5,
  fontSize: '20px',
  color: 'gray',
  flexDirection: 'column',
  lineHeight: '300px'
})

export default createComponent(info)
